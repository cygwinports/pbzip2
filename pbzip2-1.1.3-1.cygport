DESCRIPTION="Parallel BZIP2 de/compressor"
HOMEPAGE="http://www.compression.ca/pbzip2/"
SRC_URI="http://launchpad.net/pbzip2/${PV[1]}.${PV[2]}/${PV}/+download/${P}.tar.gz"

src_compile() {
	lndirs
	cd ${B}
	cygmake
}

src_install() {
	cd ${B}
	cyginstall PREFIX=${D}/usr
}
